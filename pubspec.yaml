name: juliemobiile
description : "Yasmin project."
publish_to: 'none'
 
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.0
  cloud_firestore: ^5.5.0
  firebase_auth: ^5.3.3
  google_sign_in: ^6.2.2
  cupertino_icons: ^1.0.8
  google_fonts: ^5.0.0
  share_plus: ^10.1.2
  salomon_bottom_bar: ^3.3.2
  image_picker: ^1.1.2
  firebase_storage: ^12.3.7
  table_calendar: ^3.1.3
  material_symbols_icons: ^4.2801.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true       

  assets:
    - assets/google_logo.png
